<nav class="navbar">
    <div>
        {{#if image}}
            <a class="back" href="/private/profile"><img src="/images/profileimages/{{ user.pictureOfUser }}" alt="avatar"/></a>
        {{/if}}
        {{#if wall }}
            <a class="back" href="/private/wall"><i class="fas fa-chevron-left"></i></a>
        {{/if}}

    </div>
    <div class="title">
        <h1>Be Positive</h1>
    </div>
    {{#if logout }}
        <form action="/auth/logout" method="POST">
            <button class="nav-button" type="submit">Log Out</button>
        </form>
    {{/if}}
</nav>